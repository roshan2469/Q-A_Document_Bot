# Q-A_Document_Bot
This project is a Retrieval-Augmented Generation (RAG) based document chatbot that allows users to ask questions from their own documents (PDF, TXT, DOCX). It retrieves relevant context from the documents and uses a local LLM via Ollama to generate accurate, source-grounded answers.  system runs completely offline without requiring any paid APIs.
